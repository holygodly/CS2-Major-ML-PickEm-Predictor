# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# ── 运行时产物(每次跑都会重新生成,不进版本)──
output/
logs/

# ── 实验/消融记录 ──
experiments/

# ── 本地训练数据（生成文件，不进版本；样例文件 *.example.* 保留）──
data/*.csv
data/*.json
data/*.txt
!data/*.example.*

# ── 训练产物 ──
models/*.json
models/*.pkl
models/*.csv
models/*.png

# ── 系统 ──
.DS_Store
Thumbs.db
*.bak
*.log
